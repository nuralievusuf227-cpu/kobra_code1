# 🔧 Пошаговая инструкция по установке

## Шаг 1: Подготовка системы

### Проверка версии Python
```bash
python --version
```
Должна быть **Python 3.10 или выше**

### Обновление pip
```bash
python -m pip install --upgrade pip
```

## Шаг 2: Установка зависимостей Python

```bash
cd c:\Users\Leader\Desktop\pit.py
pip install -r requirements.txt
```

Это установит:
- `aiogram` - фреймворк для Telegram ботов
- `yt-dlp` - загрузчик видео с YouTube
- `aiohttp` - асинхронный HTTP клиент
- `python-dotenv` - управление переменными окружения

## Шаг 3: Установка FFmpeg

FFmpeg необходим для конвертации видео в MP3.

### Вариант 1: Через Chocolatey (Windows, рекомендуется)
```bash
# Если Chocolatey не установлен:
# https://chocolatey.org/install

choco install ffmpeg
```

### Вариант 2: Ручная установка (Windows)
1. Скачайте с https://ffmpeg.org/download.html
2. Распакуйте архив
3. Добавьте путь к папке `bin` в переменную окружения PATH:
   - Нажмите `Win + X`
   - Выберите "Система"
   - "Дополнительные параметры системы"
   - "Переменные окружения"
   - В "Переменные среды" найдите PATH
   - Добавьте полный путь к папке `bin` FFmpeg

### Вариант 3: Через WSL (Windows)
```bash
wsl
sudo apt-get update
sudo apt-get install ffmpeg
```

### Linux
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install ffmpeg

# Fedora
sudo dnf install ffmpeg

# Arch
sudo pacman -S ffmpeg
```

### macOS
```bash
brew install ffmpeg
```

### Проверка установки FFmpeg
```bash
ffmpeg -version
```

Должна вывести версию FFmpeg.

## Шаг 4: Получение Telegram Bot Token

1. **Откройте Telegram** (Web или Desktop/Mobile)
2. **Найдите @BotFather** (официальный bot для создания ботов)
3. **Отправьте команду**: `/start`
4. **Отправьте**: `/newbot`
5. **Ответьте на вопросы**:
   - "Как назвать бота?" → Введите имя (например: `my_youtube_bot`)
   - Будет предложено имя пользователя → Примите или предложите свой
6. **Получите токен** вида:
   ```
   Done! Congratulations on your new bot. You will find it at t.me/your_bot_name. 
   You can now add a description, about section and profile picture for your bot, see /help for a list of commands.
   
   Use this token to access the HTTP API:
   123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
   ```
7. **Скопируйте токен** (сохраните в безопасном месте)

## Шаг 5: Конфигурация бота

### Способ 1: Через .env файл (рекомендуется)

1. Создайте файл `.env`:
   ```bash
   # Windows
   copy .env.example .env
   
   # Linux/macOS
   cp .env.example .env
   ```

2. Отредактируйте `.env`:
   ```
   TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
   MAX_FILESIZE_MB=50
   ```

### Способ 2: Прямое редактирование кода

Откройте `pit.py` и найдите строки:
```python
TELEGRAM_BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
```

Замените на ваш токен:
```python
TELEGRAM_BOT_TOKEN = "123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11"
```

## Шаг 6: Загрузка бота в Telegram

1. **Откройте Telegram**
2. **Найдите своего бота** по имени (например: @your_bot_name)
3. **Нажмите Start** или отправьте `/start`

## Шаг 7: Запуск бота

### Прямой запуск
```bash
python pit.py
```

Ожидаемый вывод:
```
INFO:__main__:Bot started polling...
```

### Запуск в фоне (Windows)
```bash
# Создайте файл run.bat в папке проекта:
@echo off
python pit.py
pause
```
Дважды кликните на `run.bat`

### Запуск в фоне (Linux/macOS)
```bash
nohup python pit.py > bot.log 2>&1 &
```

Для остановки:
```bash
ps aux | grep pit.py
kill <PID>
```

## Шаг 8: Тестирование

1. В Telegram отправьте боту любую YouTube ссылку:
   ```
   https://www.youtube.com/watch?v=dQw4w9WgXcQ
   ```

2. Выберите формат (MP4 или MP3)

3. Дождитесь загрузки

4. Получите файл ✅

## Возможные проблемы

### Проблема: "ModuleNotFoundError: No module named 'aiogram'"
**Решение**: 
```bash
pip install -r requirements.txt
```

### Проблема: "Invalid token or bot is not running"
**Решение**:
- Проверьте корректность токена
- Проверьте интернет соединение
- Убедитесь, что бот запущен

### Проблема: "FFmpeg not found"
**Решение**:
- Переустановите FFmpeg
- Проверьте, что FFmpeg добавлен в PATH:
  ```bash
  ffmpeg -version
  ```

### Проблема: Очень медленная загрузка
**Решение**:
- Проверьте скорость интернета
- Выберите видео меньшего размера
- Перезагрузите бота

## Дополнительные команды

### Перезагрузка зависимостей
```bash
pip install --upgrade -r requirements.txt
```

### Проверка установленных пакетов
```bash
pip list
```

### Очистка кэша pip
```bash
pip cache purge
```

## Готово! 🎉

Теперь ваш YouTube Downloader Telegram Bot полностью готов к работе!

**Следующие шаги:**
1. Запустите бота: `python pit.py`
2. Откройте Telegram и найдите своего бота
3. Начните скачивать видео и музыку!

---

Если возникли проблемы, проверьте логи или обратитесь к разделу "Troubleshooting" в README.md
